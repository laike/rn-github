/**
 * 首先引入相关的包
 */
import Api from '../../untils/api';
import Http from '../../untils/http';
import Realm from '../../dao/db';

export const searchResponsitories = (query, data) => {};

export const getTrending = (since, language) => {};
