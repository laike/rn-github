import React from 'react';
import { Text, View } from 'react-native';

const ShowCode = ({
    params,
}) => (
        <View>
            <Text>ShowCode</Text>
        </View>
    );

export default ShowCode;
